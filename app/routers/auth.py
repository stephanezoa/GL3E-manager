"""
Auth router - authentication endpoints
"""
from fastapi import APIRouter

router = APIRouter()

# Auth endpoints are handled in admin router for now
# This file is a placeholder for future auth expansion
